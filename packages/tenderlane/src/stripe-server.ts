/**
 * Stripe server-side adapter (creates Checkout Sessions and PaymentIntents).
 *
 * @example
 * ```ts
 * import { stripeServerAdapter } from 'tenderlane/stripe/server';
 * ```
 *
 * @packageDocumentation
 */
export * from '@tenderlane/stripe/server';
